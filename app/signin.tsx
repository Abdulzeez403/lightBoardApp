import React from "react";
import SignInScreen from "../src/modules/auth/signIn/screen";

const ApSignin = ({ navigate }: any) => {
  return <SignInScreen navigation={navigate} />;
};

export default ApSignin;
