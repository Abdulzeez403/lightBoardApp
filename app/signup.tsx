import React from "react";
import SignUpScreen from "../src/modules/auth/signUp/screen";

const ApSignup = ({ navigate }: any) => {
  return <SignUpScreen navigation={navigate} />;
};

export default ApSignup;
