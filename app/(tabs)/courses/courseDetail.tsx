import React from "react";
import CourseDetailScreen from "../../../src/modules/courseDetail/screen";

const courseDetail = ({ route }: any) => {
  return <CourseDetailScreen route={route} />;
};

export default courseDetail;
